apiVersion: v1
kind: ConfigMap
metadata:
  name: targeting-service-config
  namespace: toogle-master
data:
  PORT: "8003"
  AUTH_SERVICE_URL: "http://auth-service.toogle-master.svc.cluster.local:8001"

---
apiVersion: v1
kind: Secret
metadata:
  name: targeting-service-secret
  namespace: toogle-master
type: Opaque
data:
  # DATABASE_URL: postgres://user:password@rds-endpoint:5432/targeting_db
  DATABASE_URL: cG9zdGdyZXM6Ly91c2VyOnBhc3N3b3JkQHJkcy1lbmRwb2ludDo1NDMyL3RhcmdldGluZ19kYg==

---
apiVersion: v1
kind: Service
metadata:
  name: targeting-service
  namespace: toogle-master
spec:
  selector:
    app: targeting-service
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: targeting-service
  namespace: toogle-master
spec:
  replicas: 1
  selector:
    matchLabels:
      app: targeting-service
  template:
    metadata:
      labels:
        app: targeting-service
    spec:
      containers:
      - name: targeting-service
        image: <ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/targeting-service:latest
        ports:
        - containerPort: 8003
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: targeting-service-config
              key: PORT
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: targeting-service-config
              key: AUTH_SERVICE_URL
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: targeting-service-secret
              key: DATABASE_URL
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 5
