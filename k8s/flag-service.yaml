apiVersion: v1
kind: ConfigMap
metadata:
  name: flag-service-config
  namespace: toogle-master
data:
  PORT: "8002"
  AUTH_SERVICE_URL: "http://auth-service.toogle-master.svc.cluster.local:8001"

---
apiVersion: v1
kind: Secret
metadata:
  name: flag-service-secret
  namespace: toogle-master
type: Opaque
data:
  # DATABASE_URL: postgres://user:password@rds-endpoint:5432/flag_db
  DATABASE_URL: cG9zdGdyZXM6Ly91c2VyOnBhc3N3b3JkQHJkcy1lbmRwb2ludDo1NDMyL2ZsYWdfZGI=

---
apiVersion: v1
kind: Service
metadata:
  name: flag-service
  namespace: toogle-master
spec:
  selector:
    app: flag-service
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flag-service
  namespace: toogle-master
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flag-service
  template:
    metadata:
      labels:
        app: flag-service
    spec:
      containers:
      - name: flag-service
        image: <ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/flag-service:latest
        ports:
        - containerPort: 8002
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: flag-service-config
              key: PORT
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: flag-service-config
              key: AUTH_SERVICE_URL
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: flag-service-secret
              key: DATABASE_URL
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5
